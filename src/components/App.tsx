import * as React from "react";
import "./app.scss";
import AlbumListContainer from "../containers/library/AlbumListContainer";

export default (props: {}) => (
    <div className="app-root"><AlbumListContainer /></div>
);
